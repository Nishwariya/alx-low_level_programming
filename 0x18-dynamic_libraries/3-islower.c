#include "main.h"

/**
 * _islower -> check if the character is lowercase
 * @c: a character argument
 * Return: returns 1 and zero depending on condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
